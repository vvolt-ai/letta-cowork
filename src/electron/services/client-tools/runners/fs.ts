/**
 * Deprecated — superseded by ./letta_tools/index.ts which registers the
 * faithful cowork-gui-cloned tool set (Read, Write, Edit, MultiEdit,
 * Glob, Grep, LS, ApplyPatch, TodoWrite, AskUserQuestion).
 *
 * This module is intentionally empty. Safe to delete in a future commit;
 * left in place to avoid breaking any stale TypeScript build artefacts
 * that might still reference the old export names.
 */

export {};
