<!-- Deprecated. Replaced by letta_no_memfs.md (the new "standard" mode prompt
in letta-code's promptAssets.ts). Kept as an empty stub because the
sandbox doesn't permit deletion. Safe to delete in a future commit. -->
