<!-- Deprecated. Replaced by letta.md (which is now the memfs variant of the
default prompt in letta-code's promptAssets.ts). Kept as an empty stub
because the sandbox doesn't permit deletion. Safe to delete in a future
commit. -->
